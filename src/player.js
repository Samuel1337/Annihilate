export class Player {
    constructor(symbol, color, cap, rate) {
        this.symbol = symbol;
        this.color = color;
        this.cap = cap;
        this.rate = rate;
        // this.draw();
    }
    draw = function draw(ctx) {
        // ctx.fillStyle = this.color;
        
        // ctx.beginPath();
        // ctx.arc(
        //     this.pos[0], this.pos[1], this.radius, 0, 2 * Math.PI, true
        // );
        // ctx.fill();
    };
}